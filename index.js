const submit = document.getElementById('button');
const form = document.getElementById('form');

async function openForm() {}

submit.addEventListener('click', (e) => {
  e.preventDefault();
  form.getElementsByClassName('form:active');
  openForm();
});
